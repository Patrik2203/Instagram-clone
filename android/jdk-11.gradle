gradle.projectsEvaluated {
    tasks.withType(JavaCompile) {
        options.fork = true
        options.forkOptions.executable = "/usr/local/opt/openjdk@11/bin/javac"  // Path to JDK 11 javac
    }
}